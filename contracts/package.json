{
  "name": "visadorm-p2p-contracts",
  "version": "1.0.0",
  "description": "Visadorm P2P USDC Trading Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network base",
    "verify:sepolia": "hardhat run scripts/verify.js --network baseSepolia",
    "verify:mainnet": "hardhat run scripts/verify.js --network base",
    "transfer-ownership": "hardhat run scripts/transfer-ownership.js --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-ledger": "^1.0.0",
    "dotenv": "^16.4.0"
  }
}
